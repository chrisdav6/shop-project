<%- include('../partials/header') %>
<div class="container">
  <main>
    <h1>My Products</h1>
    <hr>
    <% if(products.length > 0) { %>
    <div class="row mt-5">
      <% products.forEach((product) => { %>
      <div class="col-md-4">
        <div class="card mb-5">
          <img src="<%= product.imageUrl %>" class="card-img-top" alt="<%= product.title %>">
          <div class="card-body">
            <h5 class="card-title"><%= product.title %></h5>
            <p class="card-text"><%= product.description %></p>
          </div>
          <ul class="list-group list-group-flush">
            <li class="list-group-item"><b>Price: $<%= product.price %></b></li>
          </ul>
          <div class="card-body">
            <a href="/products/<%= product.id %>" class="btn btn-primary mr-2"><i class="fas fa-info-circle"></i>
              Details</a>
            <form action="/add-to-cart" class="d-inline-block" method="POST">
              <button class="btn btn-success"><i class="fas fa-shopping-cart"></i> Add to Cart</button>
            </form>
          </div>
        </div>
      </div><!-- /col-md-4 -->
      <% }); %>
    </div><!-- /row -->
    <% } else { %>
    <p>No Products to list at the moment</p>
    <% } %>
  </main>
</div>
<%- include('../partials/footer') %>